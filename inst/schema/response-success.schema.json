{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "properties": {
        "status": {
            "enum": ["success"]
        },
        "data": {
        },
        "errors": {
            "type": "null"
        }
    },
    "required": ["status", "data", "errors"],
    "additionalProperties": false
}
